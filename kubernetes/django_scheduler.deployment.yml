apiVersion: apps/v1
kind: Deployment
metadata:
  name: django-scheduler
  labels:
    app: django-scheduler
spec:
  replicas: 1
  selector:
    matchLabels:
      app: django-scheduler
  template:
    metadata:
      labels:
        app: django-scheduler
    spec:
      containers:
        - image: jc2acr.azurecr.io/django:v1.2
          name: django-scheduler
          env:
          - name: APP_MODE
            value: "scheduler"
          envFrom:
          - secretRef:
              name: django-secret
          - configMapRef:
              name: django-config
          resources:
            limits:
              memory: "256M"
              cpu: "500m"